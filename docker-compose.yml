# Optional PostgreSQL for local development:  docker compose up -d
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: eduvoice
      POSTGRES_PASSWORD: eduvoice
      POSTGRES_DB: eduvoice
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
